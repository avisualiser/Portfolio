var splide = new Splide('.carousel1', {
    type: 'loop',
    perPage: 3,
    autoplay: true,
});

splide.mount();

var splide = new Splide('.carousel2', {
    type: 'loop',
    perPage: 3,
    autoplay: true,
});

splide.mount();

var splide = new Splide('.carousel3', {
    type: 'loop',
    perPage: 3,
    autoplay: true,
});

splide.mount();

var splide = new Splide('.carousel4', {
    type: 'loop',
    perPage: 3,
    autoplay: true,
});

splide.mount();

